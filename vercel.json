{
    "buildCommand": "npx prisma generate --schema=apps/frontend/prisma/schema.prisma && cd apps/frontend && npm run build",
    "outputDirectory": "apps/frontend/.next",
    "installCommand": "npm install",
    "framework": null
}